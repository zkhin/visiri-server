CREATE TABLE images (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  image_url TEXT DEFAULT 'No Images Provided' NOT NULL,
  image_width INTEGER DEFAULT '0' NOT NULL,
  image_height INTEGER DEFAULT '0' NOT NULL,
  experiment_id INTEGER REFERENCES experiments(id) ON DELETE CASCADE NOT NULL
);